# Puppeteer will download Chrome for Testing on `npm ci`.
# PUPPETEER_CACHE_DIR is set in Dockerfile to a Docker layer-cacheable path.
# To skip Chrome download (if providing your own Chrome binary via CHROME_PATH):
#   PUPPETEER_SKIP_DOWNLOAD=true npm ci
