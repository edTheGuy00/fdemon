# Ad-hoc website deploy (independent of release).
# The main release workflow handles website deployment automatically.
#
# To deploy manually:
#   GitHub → Actions → Publish Website → Run workflow
#
# To deploy on your server:
#   docker pull ghcr.io/edtheguy00/flutter-demon-site:latest
#   docker run -d -p 80:80 ghcr.io/edtheguy00/flutter-demon-site:latest
#
# Build stages (Dockerfile):
#   1. builder  — Rust + Trunk: wasm32 WASM SPA → dist/
#   2. prerender — Node + Puppeteer: headless Chrome snapshots all 11 routes
#                  into dist/<route>/index.html
#                  If this step fails the CSR dist/ is used as-is (fallback).
#   3. serve    — nginx: serves dist/ (with or without prerendered snapshots)
#
# To skip prerendering (emergency deploy):
#   GitHub → Actions → Publish Website → Run workflow → skip_prerender: true

name: Publish Website

on:
  workflow_dispatch:
    inputs:
      skip_prerender:
        description: 'Skip headless-Chrome prerender step (use CSR fallback)'
        required: false
        default: 'false'
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: edtheguy00/flutter-demon-site

jobs:
  build-and-push:
    name: Build & Push Website Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5.10.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=ref,event=branch
            type=sha

      - name: Build and push
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: ./website
          file: ./website/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            SKIP_PRERENDER=${{ inputs.skip_prerender == 'true' && '1' || '0' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
